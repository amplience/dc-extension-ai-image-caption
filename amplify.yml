version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 18
        - nvm use 18
        - npm install
    build:
      commands:
        - CI=false npm run build
  artifacts:
    baseDirectory: build
    files:
      - "**/*"
